"use client";
import ProjectDetail from '@/tanstack/project/project-detail';

export default function AppProjectPage() {
  // ProjectDetail reads the ?id= query param and displays the project
  return <ProjectDetail />;
}
